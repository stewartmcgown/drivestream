/**
 * Options for running the indexer
 */
export interface IndexerOptions {
    
    libraryId: string;
}

/**
 * Search a library for changes in file content
 * 
 * @param options 
 */
export const searchLibrary = (options: IndexerOptions) => {

}